# Auth errors are classified before debugging code

The learner can separate missing credentials, expired SSO, access denied, and model-routing failures. Future operational lessons can start by asking which layer produced the error.
